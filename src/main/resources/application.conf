ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ com.nhstrial.ApplicationKt.module ]
    }
}

database {
    url = "jdbc:postgresql://localhost:5435/nhstrial"
    url = ${?DATABASE_URL}
    username = "nhstrial"
    username = ${?DATABASE_USERNAME}
    password = "nhstrial"
    password = ${?DATABASE_PASSWORD}
}
